# Wiedervorlage

## Parked Improvement Items

These items were identified during the share, referral, and email-marketing review but were not part of the current implementation batch.

1. Sign public share snapshots server-side so manipulated payloads can be rejected.
2. Add CSRF protection for authenticated write routes beyond SameSite cookies.
3. Introduce an explicit schema-migration ledger so migrations run once instead of relying only on idempotent SQL.
4. Add Playwright coverage for share-card visual regressions, referral persistence, and public preview copy behavior.
5. Expand the email-marketing UI with richer segmentation, searchable recipient history, and campaign performance charts.
